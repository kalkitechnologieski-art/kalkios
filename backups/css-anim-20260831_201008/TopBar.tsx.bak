'use client'
import { Menu } from 'lucide-react'
import Image from 'next/image'
import Link from 'next/link'
import { NotificationBell } from '@/components/notifications/NotificationBell'

export function TopBar({ onMenuClick }: { onMenuClick: () => void }) {
  return (
    <header className="fixed top-0 safe-area-top left-0 right-0 z-50 h-14 bg-black/90 backdrop-blur-xl border-b border-cyan/10 flex items-center justify-between px-4 md:px-6">
      <button
        onClick={onMenuClick}
        className="p-3 -ml-3 rounded-full hover:bg-white/5 transition group"
        aria-label="Open menu"
      >
        <Menu className="w-6 h-6 text-cyan/60 group-hover:text-cyan transition" />
      </button>

      <Link href="/" className="flex items-center gap-3 group">
        <div className="relative w-8 h-8">
          <div className="absolute inset-0 rounded-full bg-cyan/20 blur-xl group-hover:blur-2xl transition" />
          <Image
            src="/images/logo.svg"
            alt="KALKI OS"
            width={32}
            height={32}
            className="object-contain drop-shadow-glow group-hover:drop-shadow-glow-strong transition"
            priority
          />
        </div>
        <div className="flex flex-col leading-tight">
          <span className="text-sm font-semibold tracking-wider text-transparent bg-clip-text bg-gradient-to-r from-cyan to-purple-400 animate-shimmer">
            KALKI INTELLIGENCE
          </span>
          <span className="text-[8px] text-cyan/30 tracking-[0.3em] uppercase font-mono">
            ● CYBERPUNK EDITION
          </span>
        </div>
      </Link>

      <div className="flex items-center gap-1">
        <NotificationBell />
        <div className="w-8 h-8 rounded-full bg-gradient-to-br from-cyan to-purple-600 flex items-center justify-center text-xs font-bold text-black shadow-glow border border-cyan/30 ml-1 hover:scale-105 transition-transform duration-200 cursor-pointer">
          U
        </div>
      </div>
    </header>
  )
}
